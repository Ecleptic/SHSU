Transport layer Handles Communication between processes.

network layer uses Forwarding and Routing to send a packet to the right destination. 

Ch1. had forwarding. For moving packets from Router's input to the appropriate router output.
Routing: Determines route taken by packets from source to destination.

IP datagrams now have version numbers. I'm assuming IPv6 or IPv4 

a hop is 1 link (duh)
TTL is just like the timeout window.
