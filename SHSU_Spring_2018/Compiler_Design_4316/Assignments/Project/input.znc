program
    var N : integer;
    var SQRT : integer;
begin
    N := readInt;
    SQRT := 0;

    while SQRT * SQRT <= N loop
        SQRT := SQRT + 1;
    endwhile;

    SQRT := SQRT - 1;

    writeInt(SQRT);
end
